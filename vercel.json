{
  "installCommand": "corepack enable && pnpm install --no-frozen-lockfile --prod=false",
  "buildCommand": "cd /vercel/path0 && pnpm --filter @workspace/api-server run build && pnpm --filter @workspace/interfreight run build",
  "outputDirectory": "artifacts/interfreight/dist/public",
  "functions": {
    "api/index.mjs": {
      "maxDuration": 60,
      "includeFiles": "artifacts/api-server/dist/**"
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
